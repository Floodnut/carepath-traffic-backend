version: "3.0"
services: 
  app:
    build: .
    container_name: Routing_Server_NodeJS
    image: routing_server:latest
    ports: 
      - "9001:9001"
    environment:
      NODE_ENV: localhost
    volumes:
      - /source:/source
      - /source/node_modules
    restart: always